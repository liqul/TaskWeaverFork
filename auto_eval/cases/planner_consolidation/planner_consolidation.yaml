version: 0.1
config_var:
  execution_service.kernel_mode: "local"
app_dir: ../project/
data_files:
  - demo_data.csv
task_description: |-
  Calculate the mean and std of the 'Count' column in ./demo_data.csv, and count how many values are larger than 3 std from the mean.
  Please first provide a concise multi-step plan without executing it.
  Then, after I confirm, go ahead and execute the plan and return the result.
scoring_points:
  - score_point: "The correct mean value is near 78172 and the correct std value is near 16233"
    weight: 1
  - score_point: "There should be 5 data points that are larger than 3 std from the mean"
    weight: 1
  - score_point: "Data loading and mean/std calculating should be presented within a single step"
    weight: 1
